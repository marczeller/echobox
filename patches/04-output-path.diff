# Patch 04: Configurable Output Path
# Target: cli.py (in trnscrb package)
#
# PROBLEM:
# trnscrb writes transcripts to a hardcoded directory. Echobox needs
# transcripts in ~/echobox-data/transcripts/ for pipeline processing.
#
# CHANGE:
# 1. Add a new CLI argument:
#
#    parser.add_argument(
#        "--output-dir",
#        help="Directory for transcript output files",
#        default=os.environ.get("TRNSCRB_OUTPUT_DIR", "~/meeting-notes")
#    )
#
# 2. Where the transcript file path is constructed, use the configured
#    output directory instead of the default:
#
#    output_dir = Path(args.output_dir).expanduser()
#    output_dir.mkdir(parents=True, exist_ok=True)
#    transcript_path = output_dir / f"{transcript_id}.txt"
#
# WHY:
# Separating the output directory lets Echobox manage its own data
# structure without interfering with trnscrb's defaults.
